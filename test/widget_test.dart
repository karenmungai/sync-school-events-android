import 'package:flutter_test/flutter_test.dart';

import 'package:sync_school_events/main.dart';

void main() {
  test('app widget can be created', () {
    expect(const SyncSchoolEventsApp(), isNotNull);
  });
}
